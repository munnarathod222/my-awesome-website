{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others --raw \"npm run dev --prefix apps/web\" \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/pocketbase\"",
    "build:client": "npm --prefix apps/web run build && node sync_dist_fallbacks.js",
    "build": "node sync_dist_fallbacks.js",
    "start": "npm run start --prefix apps/api",
    "lint": "concurrently --raw \"npm run lint --prefix apps/web\" \"npm run lint --prefix apps/api\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.2.0",
    "cors": "^2.8.5",
    "express": "^5.0.1",
    "express-rate-limit": "^8.3.1",
    "helmet": "^8.0.0",
    "isomorphic-git": "^1.38.7",
    "jspdf": "^4.2.1",
    "jspdf-autotable": "^5.0.8",
    "morgan": "^1.10.0",
    "multer": "^2.1.1",
    "node-cron": "^4.5.0",
    "nodemailer": "^9.0.3",
    "pocketbase": "^0.27.0",
    "vite": "^7.3.1"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "workspaces": [
    "apps/*"
  ]
}
